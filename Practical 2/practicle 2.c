#include<stdio.h>
int main()
{
    int num;
    //num=10;
    printf("enter the number: ");
    scanf("%d",&num);
    printf("this is number %d",num);
    printf("the size of integer is: %d",sizeof(float));
    return 0;
}
